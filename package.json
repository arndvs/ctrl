{
  "name": "ctrlshft-dashboard",
  "version": "0.2.0",
  "description": "ctrl+shft compliance dashboard daemon",
  "private": true,
  "main": "bin/dashboard-daemon.js",
  "scripts": {
    "start":   "node bin/dashboard-daemon.js",
    "dev":     "node bin/dashboard-daemon.js --debug",
    "stop":    "bash bin/start-dashboard.sh --stop",
    "status":  "bash bin/start-dashboard.sh --status"
  },
  "optionalDependencies": {
    "better-sqlite3": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
